// Generado en runtime por /docker-entrypoint.d/40-runtime-config.sh
// a partir de la variable de entorno API_BASE_URL del contenedor.
window.__RUNTIME_CONFIG__ = {
  apiUrl: "${API_BASE_URL}/api",
};
